# cleanup .DS_Store files, created by Mac OS X finder.

function remove_ds_store() {
	if [ -z "$(find . -name .DS_Store -print0)" ]; then
		echo "No .DS_Store found."
	else
		find . -name .DS_Store -print0 | xargs -0 rm -v
	fi
}
# vim:set ft=zsh tw=78:
